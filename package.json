{
  "name": "hyperload",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@8.15.9",
  "scripts": {
    "build": "pnpm -r --filter \"@hyperload/*\" build",
    "typecheck": "pnpm -r --filter \"@hyperload/*\" typecheck",
    "test": "pnpm --filter \"@hyperload/gtfs-v4-struct\" test && pnpm --filter \"@hyperload/gtfs-v4-sql-load\" test"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^3.2.4"
  }
}
